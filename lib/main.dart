// material是指Google推行的设计风格
import 'package:flutter/material.dart';

void main(){
  runApp(
    Center(
      child: Text(
        'hello',
        textDirection: TextDirection.ltr,
      )
    )
  );
}